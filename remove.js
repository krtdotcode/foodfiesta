function remove1(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 150}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 150;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove2(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 280}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 280;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove3(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 240}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 240;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove4(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 300}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 300;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove5(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 85}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 85;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove6(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 90}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 90;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove7(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 130}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 130;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove8(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 60}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 60;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove9(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 100}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 100;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove10(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 90}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 90;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove11(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 110}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 110;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove12(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 60}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 60;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove13(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 80}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 80;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove14(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 75}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 75;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}

function remove15(index) {
    cartListArr.splice(index, 1);
    totalElem.innerHTML = `Total: &#8369 ${(total.toFixed(2)) - 70}`;
    document.querySelector(".cart-lists").innerHTML = cartListArr.join("\n");
    total -= 70;
    document.querySelector(".cart-lists p").innerHTML = `Total: &#8369 ${(total.toFixed(2))}`;
}